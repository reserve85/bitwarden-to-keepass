services:
  bitwarden-to-keepass:
    build: .
    command: ["/bin/bash", "entrypoint.sh"]
    volumes:
      - .:/bitwarden-to-keepass:z
      - ./exports:/exports:z
      - 'bw-config:/root/.config/Bitwarden CLI'
    env_file:
      - .env

volumes:
  bw-config:
